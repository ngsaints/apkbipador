import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.base44.wmsrocket',
  appName: 'WMS Rocket',
  webDir: 'mobile-bipador'
};

export default config;
